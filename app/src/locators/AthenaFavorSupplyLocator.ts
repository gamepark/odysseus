import { PileLocator } from '@gamepark/react-game'

// A generic supply pile, off the ship board.
export const athenaFavorSupplyLocator = new PileLocator({ coordinates: { x: -30, y: -20 }, radius: 2 })
