// Cinzel echoes the chiseled-stone look of the box's "ODYSSEUS" logo lettering, for titles and
// interactive labels. Nunito stays for body text, matching the sans-serif already used across the org's
// other Game Park adaptations (see ../dragon-bomb).
export const fontDisplay = `'Cinzel', Georgia, serif`
export const fontBody = `'Nunito', 'Segoe UI', sans-serif`
