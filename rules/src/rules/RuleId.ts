export enum RuleId {
  /** Also resolves "go on adventure or rest" for the chosen card in the same move (see ChooseTrialCardRule). */
  ChooseTrialCard = 1,
  ResolveSkillGain,
  ChooseTale,
  FinishTurn
}
